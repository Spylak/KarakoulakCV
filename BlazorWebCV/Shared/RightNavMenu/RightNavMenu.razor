<MudSpacer/>
<MudList Clickable="true" Class="my-2">
    @foreach (var item in NavMenuItems.SkipLast(1))
    {
        <MudListItem Text="@item.Text"
                     OnClick="@(() => ScrollTo(item.Section))"
                     Icon="@item.Icon"
                     IconColor="Color.Default"/>
    }
</MudList>
<MudSpacer/>